# 小程序页面拆解文档（供AI生成UI代码）

> 目标：让大模型根据以下拆解，直接生成可运行的HTML/CSS/JS代码
> 风格：微信小程序风格，白色背景，绿色主题色（#07C160）

***

## 一、全局样式规范（所有页面共用）

### 1.1 颜色变量

| 变量名  | 色值      | 用途           |
| ---- | ------- | ------------ |
| 主题绿  | #07C160 | 主要按钮、选中态、品牌色 |
| 主题绿浅 | #e8f8ef | 绿色背景区块       |
| 红色   | #FA5151 | 姨妈、警告、红点     |
| 橙色   | #FF8C00 | 次要强调         |
| 紫色   | #9C27B0 | 稀有徽章         |
| 蓝色   | #10AEFF | 信息、链接        |
| 灰色背景 | #f5f5f5 | 页面底色         |
| 白色   | #fff    | 卡片底色         |
| 分割线  | #e5e5e5 | 边框           |
| 次要文字 | #999    | 辅助说明         |
| 主要文字 | #333    | 正文           |

### 1.2 圆角规范

| 元素类型 | 圆角        |
| ---- | --------- |
| 卡片   | 12px      |
| 按钮   | 24px（胶囊）  |
| 图标区  | 16px      |
| 日历格子 | 8px / 50% |

### 1.3 间距规范

| 位置    | 间距   |
| ----- | ---- |
| 左右边距  | 16px |
| 卡片内边距 | 16px |
| 元素间距  | 12px |

### 1.4 字体规范

| 用途   | 字号      | 字重  |
| ---- | ------- | --- |
| 导航标题 | 17px    | 600 |
| 卡片标题 | 16px    | 600 |
| 正文   | 14px    | 400 |
| 辅助文字 | 11-12px | 400 |
| 大数字  | 36-48px | 800 |

***

## 二、首页（日历+快捷记录）

### 2.1 页面结构（从上到下）

```
1. 状态栏（时间+电量）
2. 导航栏 - 标题「记录生活」
3. 跑马灯横幅 - 滚动文案
4. 日历标题行 - 「2026年5月」+ 翻页箭头
5. 星期行 - 日 一 二 三 四 五 六
6. 日历格子 - 6行×7列
7. 今日状态卡片 - 3个状态块
8. 快捷记录区标题 - 「快速记录」
9. 快捷记录第一行 - 💩拉屎、🩸姨妈（固定）
10. 展开/收起按钮
11. 折叠区 - 其余记录类型（默认隐藏）
12. Tab Bar - 日历/计时/统计/我的
```

### 2.2 跑马灯横幅

**视觉：**

- 渐变背景：`linear-gradient(90deg, #667eea, #764ba2)`
- 白色文字，字号13px，字重600
- 圆角20px，边距12px 16px
- 内部文字横向无限滚动

**文案内容（动态）：**

```
🎁 邀请30位好友，解锁定制影视剧网盘资源自动推送功能！当前进度：{invite_count}/30，还差{30-invite_count}人 → 点击邀请好友 →
```

**交互：**

- 点击整个横幅 → 跳转邀请中心页面

### 2.3 日历区

**视觉：**

- 标题行：字体18px粗，右侧绿色「◀ ▶」按钮
- 星期行：11px灰色字，居中对齐
- 日历格子：正方形（aspect-ratio:1），flex居中
- 今日格子：绿色圆形背景，白色字
- 有记录的格子：右下角5px红色圆点

**数据结构（示例）：**

```javascript
calendarData = {
  year: 2026,
  month: 5,
  today: 17,
  days: [/* 日期数组，含是否有记录标记 */],
  records: [6,7,9] // 有记录的日期
}
```

**交互：**

- 点击「◀」→ 上一个月
- 点击「▶」→ 下一个月
- 点击日期格子 → 弹出该日记录详情（可选）

### 2.4 今日状态卡片

**视觉：**

- 3列等宽，每列有圆角背景（10px）
- 第一列：绿色背景 `#e8f8ef`，💩图标，文字「✓ 已记录」
- 第二列：红色背景 `#ffebee`，🩸图标，文字「第3天」
- 第三列：灰色背景 `#f8f8f8`，😴图标，如果未解锁则整体透明度0.4，文字「???」

**条件渲染：**

```javascript
// 第三列（睡眠）
if (unlocked.includes('sleep')) {
  显示「😴 + 睡眠分数」
} else {
  显示「😴 + ??? + 透明度0.4」
}
```

### 2.5 快捷记录区

#### 第一行（固定显示）

- 4列网格布局，但只显示2个有效项（💩、🩸）
- 每项：48px圆角方块 + 11px标签文字
- 方块背景：💩用绿色 `#e8f8ef`，🩸用红色 `#ffebee`

#### 折叠区（默认隐藏，点击展开后显示）

- 同样4列网格
- 显示所有未解锁的记录类型
- **未解锁样式：** 图标透明度0.4，灰度滤镜，标签显示「???」
- **已解锁样式：** 正常彩色，标签显示正常文字

**记录类型完整列表：**

| 图标  | 解锁人数 | 显示名 | 备注     |
| --- | ---- | --- | ------ |
| 💩  | 0    | 拉屎  | 默认     |
| 🩸  | 0    | 姨妈  | 默认     |
| 😴  | 1    | 睡眠  | <br /> |
| 💧  | 3    | 喝水  | <br /> |
| 📺  | 8    | 追番  | 追番提醒   |
| 🏋️ | 15   | 运动  | <br /> |
| 📚  | 15   | 学习  | <br /> |
| ✅   | 15   | 习惯  | <br /> |
| 💊  | 30   | 吃药  | <br /> |
| 😊  | 30   | 心情  | <br /> |
| 🤧  | 30   | 感冒  | <br /> |
| 🐾  | 30   | 宠物  | <br /> |
| 💕  | 30   | 恋爱  | <br /> |

**交互：**

- 点击已解锁的记录 → Toast「记录成功」→ 刷新今日状态 + 日历红点
- 点击未解锁（???）→ 弹出锁定提示弹窗

### 2.6 展开/收起按钮

**视觉：**

- 居中，绿色文字13px，字重600
- 箭头 ▼ 或 ▲
- 点击时箭头旋转180度

**状态：**

| 状态 | 文字             | 箭头 | 折叠区 |
| -- | -------------- | -- | --- |
| 收起 | 展开全部（18+ 记录类型） | ▼  | 隐藏  |
| 展开 | 收起记录类型         | ▲  | 显示  |

### 2.7 Tab Bar

**视觉：**

- 固定在底部，高度50px
- 白色背景，顶部分割线
- 4项等宽，每项：图标(20px) + 文字(10px)
- 选中态：主题绿色

**Tab列表：**

| 序号 | 图标 | 文字 | 对应页面            |
| -- | -- | -- | --------------- |
| 1  | 📅 | 日历 | home            |
| 2  | ⏱️ | 计时 | timer（暂用home代替） |
| 3  | 📊 | 统计 | stats           |
| 4  | 👤 | 我的 | profile         |

### 2.8 锁定提示弹窗

**触发条件：** 点击任意未解锁（???）记录类型

**视觉：**

- 半透明黑色遮罩（z-index高）
- 底部半屏弹窗，白色背景，上圆角16px
- 顶部：🔒大图标
- 标题：「还有更多功能？」
- 描述：「邀请好友解锁更多记录类型」
- 进度卡片：显示当前邀请人数/目标人数 + 进度条
- 按钮：绿色「立即邀请好友」
- 链接：「稍后再说」

**交互：**

- 点击遮罩或「稍后再说」→ 关闭弹窗
- 点击「立即邀请好友」→ 关闭弹窗 + 跳转邀请中心

***

## 三、邀请中心页面

### 3.1 页面结构

```
1. 导航栏 - 「邀请好友」
2. Hero区 - 邀请人数大数字 + 进度条
3. 阶梯奖励列表 - 各解锁档位及状态
4. 分享卡片预览 - 邀请卡片样式
5. 操作按钮 - 「邀请微信好友」「生成海报」
```

### 3.2 Hero区

**视觉：**

- 渐变背景 `linear-gradient(135deg, #07C160, #10AEFF)`
- 白色文字，内边距24px 16px
- 大数字48px粗体
- 进度条：白色半透明背景，白色填充

**内容：**

```
已邀请好友
{invite_count}

还差 {30-invite_count} 人解锁网盘资源自动推送
[进度条 width={(invite_count/30)*100}%]
```

### 3.3 阶梯奖励列表

**视觉：**

- 白色卡片，圆角12px，内边距16px
- 每项：左侧圆形序号 + 右侧解锁内容 + 右侧状态标签
- 序号样式：
  - 已达成：绿色背景白色字「✓」或数字
  - 未达成：灰色背景深灰字数字

**奖励阶梯：**

| 人数 | 解锁内容     | 状态判断                |
| -- | -------- | ------------------- |
| 1  | 睡眠记录     | invite\_count >= 1  |
| 3  | 喝水提醒     | invite\_count >= 3  |
| 8  | 追番提醒     | invite\_count >= 8  |
| 15 | 追番预告     | invite\_count >= 15 |
| 30 | 网盘资源自动推送 | invite\_count >= 30 |

### 3.4 分享卡片预览

**视觉：**

- 白色背景，绿色虚线边框2px
- 内边距20px，圆角12px
- 顶部：绿色粗体「还差X人，就能解锁...」
- 中部：描述文案
- 底部：绿色胶囊按钮「再邀X人」

**内容：**

```
标题：还差{30-invite_count}人，就能解锁定制影视剧网盘资源自动推送
描述：邀请30人，解锁【网盘资源自动推送】，最新番剧更新自动推送到手
按钮：⚡ 再邀{30-invite_count}人
```

### 3.5 操作按钮

**视觉：**

- 两个按钮纵向排列，间距10px
- 第一个：绿色背景，白色字「📤 邀请微信好友」
- 第二个：白色背景，绿色边框，绿色字「🖼️ 生成分享海报」

**交互：**

- 点击「邀请微信好友」→ 调用微信分享接口（前端模拟alert）
- 点击「生成海报」→ 跳转海报生成页面

***

## 四、守护者页面

### 4.1 页面结构

```
1. 导航栏 - 「健康守护者」
2. 说明文案 - 简短描述
3. 我授权的人（TA能看到我）- 区块
4. 守护我的人（我能看到TA）- 区块
5. 操作按钮 - 「发起守护授权」
```

### 4.2 授权列表项

**视觉：**

- 白色卡片内，每项左右排列
- 左侧：圆形头像（40px），不同颜色背景，文字图标
- 中间：姓名 + 关系 + 权限说明
- 右侧：操作按钮

**「我授权的人」每项结构：**

```
左侧头像：👨（男朋友）→ 蓝色背景
中间：
  - 姓名「男朋友」+ 橙色标签「暖男皮肤」
  - 副文字「男朋友 · 可查看：姨妈、睡眠」
右侧：红色「解除」文字按钮
```

**「守护我的人」每项结构：**

```
左侧头像：👩（妈妈）→ 绿色背景
中间：
  - 姓名「妈妈」
  - 副文字「妈妈」
右侧：绿色「👁️ 查看日历」文字按钮
```

### 4.3 空状态

如果列表为空：

- 显示灰色提示文字「暂无授权关系」
- 底部按钮高亮引导

### 4.4 发起授权按钮

**视觉：**

- 绿色大按钮，圆角24px
- 文字：「🤝 发起守护授权」
- 边距16px

**交互：**

- 点击 → 跳转分享/选择联系人（模拟alert）

***

## 五、情侣绑定页面

### 5.1 页面结构

```
1. 导航栏 - 「情侣日历」
2. 同步率卡片 - 渐变背景
3. 双人头像区 - 左右并排，中间爱心动画
4. 统计数据卡片 - 共同记录天数、姨妈重叠天数
5. 操作按钮 - 「生成同步率海报」
```

### 5.2 同步率卡片

**视觉：**

- 渐变背景 `linear-gradient(135deg, #FF6B9D, #C44DFF)`
- 白色文字，圆角16px，内边距24px
- 同步率数字64px粗体
- 副标题「我跟你拉屎同步率91%…你敢测吗？」

**数据：**

```
同步率 = (情侣共同有拉屎记录的天数 / 总天数) * 100%
示例：91%
```

### 5.3 双人头像区

**视觉：**

- 居中排列
- 左侧头像：👩，白色边框2px，52px
- 中间：💕 爱心图标，带心跳动画（scale 1 → 1.15 → 1）
- 右侧头像：👨，样式同左侧

**下方文字：**

```
绑定 {days} 天 · 已解锁情侣皮肤
示例：绑定 128 天
```

### 5.4 统计数据卡片

**视觉：**

- 白色卡片，内边距16px
- 2列等宽布局，灰色背景块，圆角8px
- 数字粗体20px，标签11px灰色

**数据：**

```
共同记录：{common_days}天
姨妈重叠：{period_overlap}天
```

### 5.5 生成海报按钮

**视觉：**

- 白色背景，粉红色边框 `#FF6B9D`
- 粉红色文字「🖼️ 生成同步率海报」
- 圆角24px

**交互：**

- 点击 → 跳转海报生成页面（情侣模板）

***

## 六、家族健康树页面

### 6.1 页面结构

```
1. 导航栏 - 「家族健康树」
2. 树的可视化区 - 渐变背景，大树图标
3. 树状态信息 - Lv等级 + 已解锁功能
4. 家庭成员列表
5. 操作按钮 - 「邀请家人加入」
```

### 6.2 树的可视化区

**视觉：**

- 渐变背景 `linear-gradient(180deg, #e8f5e9, #c8e6c9)`
- 圆角16px，内边距24px
- 大树图标80px
- 下方文字：树等级 + 已解锁功能

**数据：**

```
等级 = floor(家庭成员数 / 3) + 1，最大5级
已解锁功能：
  - 1级：基础健康树
  - 2级：家庭药箱提醒
  - 3级：健康周报
```

### 6.3 家庭成员列表

**视觉：**

- 白色背景，圆角12px
- 每项：头像 + 姓名 + 最近活动 + 状态标签
- 头像背景色区分角色

**数据示例：**

```
爸爸 | 血压记录 · 3天前 | 标签「健康」
妈妈 | 睡眠记录 · 昨天 | 标签「健康」
我   | 今日已记录     | 标签「本人」
```

### 6.4 邀请按钮

**视觉：**

- 绿色按钮，文字「🌱 邀请家人加入」
- 边距16px

***

## 七、追番中心页面

### 7.1 页面结构

```
1. 导航栏 - 「追番中心」+ 右侧「+订阅」
2. 提醒状态横幅 - 已解锁/未解锁状态
3. Tab栏 - 我的订阅 / 番剧库 / 更新表
4. 番剧卡片列表
5. 网盘资源横幅（仅30人解锁后显示）
```

### 7.2 提醒状态横幅

**视觉：**

- 渐变背景 `linear-gradient(135deg, #667eea, #764ba2)`
- 白色文字，圆角12px，内边距12px 16px
- 左侧图标📺，右侧文字

**条件渲染：**

```javascript
if (invite_count >= 8) {
  显示：「追番提醒已解锁 · 已订阅5部 · 今日更新2部」
} else {
  显示：「邀请8位好友解锁追番提醒 · 当前进度{invite_count}/8」
}
```

### 7.3 Tab栏

**视觉：**

- 白色背景，3项等宽
- 选中态：绿色文字 + 绿色下划线2px
- 未选中：灰色文字

### 7.4 番剧卡片

**视觉：**

- 白色背景，圆角12px，横向布局
- 左侧封面区：100px宽，渐变背景，📺图标
- 右侧信息区：番剧名 + 更新信息 + 今日更新标签

**数据示例：**

```javascript
anime = {
  name: "咒术回战 第三季",
  updateDay: "周六",
  episode: 8,
  isToday: true
}
```

**今日更新标签：**

- 绿色背景 `#e8f8ef`，绿色文字「🟢 今日更新」

### 7.5 网盘资源横幅（条件显示）

**显示条件：** `invite_count >= 30`

**视觉：**

- 渐变背景 `linear-gradient(135deg, #FFD93D, #FF6B6B)`
- 白色文字，圆角12px，内边距14px
- 标题：「🎁 番剧网盘资源」
- 副标题：「解锁！当日更新番剧网盘链接免费看」
- 底部：「查看网盘」按钮（半透明白色背景）

***

## 八、徽章墙页面

### 8.1 页面结构

```
1. 导航栏 - 「我的徽章」
2. 进度汇总 - 已获得/总数 + 进度条
3. 已解锁徽章区块
4. 未解锁徽章区块
5. 徽章网格 - 3列网格
```

### 8.2 进度汇总

**视觉：**

- 居中对齐，灰色文字
- 数字绿色粗体

**内容：**

```
已获得 {owned_badges} / {total_badges} 枚徽章
进度条宽度 {(owned_badges/total_badges)*100}%
```

### 8.3 徽章网格项

**视觉：**

- 白色卡片，圆角12px，内边距16px 8px
- 图标36px
- 名称12px粗体
- 已解锁/未解锁状态区分

**已解锁样式：**

- 正常显示
- 稀有徽章右上角带「稀有」标签（紫色）

**未解锁样式：**

- 整体透明度0.4 + 灰度滤镜
- 显示解锁条件小字

**数据示例：**

```javascript
badges = [
  { name: "连续拉屎7天", icon: "💩", rarity: "rare", unlocked: true },
  { name: "裂变之王", icon: "🏆", unlocked: false, condition: "邀请10人解锁" }
]
```

***

## 九、我的页面

### 9.1 页面结构

```
1. 导航栏 - 「我的」
2. 个人信息区 - 头像 + 昵称 + ID + 邀请人数
3. 菜单列表 - 多个菜单项
```

### 9.2 个人信息区

**视觉：**

- 渐变背景 `linear-gradient(135deg, #07C160, #05a14d)`
- 白色文字，内边距24px 16px
- 左侧：头像52px，白色半透明边框
- 中间：昵称18px粗体 + ID 11px
- 右侧：邀请人数（数字18px粗体，标签11px）

### 9.3 菜单列表

**视觉：**

- 白色卡片，圆角12px
- 每项：左侧图标 + 中间文字 + 右侧箭头
- 图标统一20px，颜色区分

**菜单项：**

| 图标  | 文字    | 右侧信息        | 跳转       |
| --- | ----- | ----------- | -------- |
| 🎁  | 邀请好友  | 还差23人（红色气泡） | invite   |
| 🛡️ | 健康守护者 | 2人          | guardian |
| 💑  | 情侣日历  | 同步率91%      | couple   |
| 📺  | 追番中心  | 已解锁（绿色）     | anime    |

***

## 十、统计页面

### 10.1 页面结构

```
1. 导航栏 - 「数据统计」
2. Tab栏 - 本月/上月/全部
3. 数据概览卡片 - 4个关键指标
4. 趋势图表 - 拉屎趋势柱状图
```

### 10.2 数据概览卡片

**视觉：**

- 白色卡片，圆角12px
- 2×2网格
- 每个指标：大数字 + 标签

**数据：**

| 指标   | 示例值 | 颜色      |
| ---- | --- | ------- |
| 拉屎次数 | 17  | #07C160 |
| 睡眠均分 | 81  | #FA5151 |
| 连续记录 | 7   | #FF8C00 |
| 击败用户 | 92% | #9C27B0 |

### 10.3 趋势图表

**视觉：**

- 白色卡片
- 标题「💩 拉屎趋势」
- 柱状图：高度代表次数，绿色柱子，灰色背景

**数据：**

```javascript
trend = [4,7,5,8,3,6,5]  // 最近7天次数
柱子高度 = (次数 / max(trend)) * 100%
```

***

## 十一、全局组件：锁定提示弹窗

### 11.1 触发条件

- 点击任何未解锁的「???」记录类型
- 点击任何需要邀请解锁的功能入口

### 11.2 弹窗结构

```
遮罩层（半透明黑，点击关闭）
  └── 内容区（底部半屏，白色，上圆角）
      ├── 🔒 大图标
      ├── 标题：「还有更多功能？」
      ├── 描述：「邀请好友解锁更多记录类型」
      ├── 进度卡片：
      │   ├── 文字：「我的邀请进度」
      │   ├── 数字：{invite_count} / {target}
      │   └── 进度条：宽度 {invite_count/target*100}%
      ├── 按钮：「📤 立即邀请好友」
      └── 链接：「稍后再说」
```

### 11.3 动态数据

| 点击场景  | target | 描述           |
| ----- | ------ | ------------ |
| 睡眠记录  | 1      | 邀请1位好友解锁睡眠记录 |
| 喝水提醒  | 3      | 邀请3位好友解锁喝水提醒 |
| 追番提醒  | 8      | 邀请8位好友解锁追番提醒 |
| 任意??? | 当前解锁阈值 | 通用文案         |

***

## 附录：数据结构汇总

```javascript
// 全局用户数据
user = {
  id: "xxx",
  name: "小明",
  avatar: "👤",
  invite_count: 7,        // 当前邀请人数
  invite_target: 30,      // 目标人数（固定）
  unlocked_types: ["poop", "period"],  // 已解锁的记录类型
  badges_owned: 4,        // 已获得徽章数
  couple: {
    bound: true,
    partner_name: "男朋友",
    sync_rate: 91,
    days: 128
  },
  family: {
    members: ["爸爸", "妈妈"],
    level: 1
  },
  today_status: {
    poop: true,           // 今日是否已拉屎
    period: 3             // 姨妈第几天，0表示没有
  }
}

// 记录类型配置
recordTypes = [
  { id: "poop", icon: "💩", name: "拉屎", unlock_level: 0 },
  { id: "period", icon: "🩸", name: "姨妈", unlock_level: 0 },
  { id: "sleep", icon: "😴", name: "睡眠", unlock_level: 1 },
  { id: "water", icon: "💧", name: "喝水", unlock_level: 3 },
  { id: "anime", icon: "📺", name: "追番", unlock_level: 8 },
  { id: "sport", icon: "🏋️", name: "运动", unlock_level: 15 },
  { id: "study", icon: "📚", name: "学习", unlock_level: 15 }
  // ... 更多
]

// 解锁阈值（运营可配）
unlockThresholds = [
  { count: 1, types: ["sleep"] },
  { count: 3, types: ["water"] },
  { count: 8, types: ["anime"] },
  { count: 15, types: ["sport", "study"] },
  { count: 30, types: ["pill", "mood", "cold", "pet", "love"] }
]
```

***

**文档结束**

复制保存为 `ui-spec.md`，即可分段喂给大模型生成完整前端代码。
